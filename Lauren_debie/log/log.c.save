//volgorde log
//datum d:m:j//u:m:s//temp:..//licht:..//

#include <stdio.h>
#include <time.h>
#define FILE_LOC "/home/debie"

struct log_data_styl
{
	char times[20];
	char temp[10];
	char licht[10];
};

void time_set(char *);
void log_set(struct log_data_styl *);

int main(void)
{
	char times[20];
	time_set(times);

	return 0;
}

void time_set(char *times)
{
	time_t temp_time = time(NULL);
	struct tm *my_date = localtime(&temp_time);
    	char date_string[11];
    	sprintf(date_string, "%02d:%02d:%02d", my_time->tm_mday, my_time->tm_mon + 1, my_time->tm_year + 1900);
    	printf("Current date: %s\n", date_string);
}


void log_set(struct log_data_styl *data)
{


}
